import { Text, View } from "react-native";
import "../global.css";

const HomeScreen = () => {
  return (
    <View>
      <Text className={"text-red-600"}>Highlights App</Text>
    </View>
  );
};

export default HomeScreen;
