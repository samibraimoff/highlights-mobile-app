import { Text, View } from "react-native";

const NotificationsScreen = () => {
  return (
    <View className={"flex justify-center items-center"}>
      <Text className={"text-red-600 text-2xl"}>Notifications</Text>
    </View>
  );
};

export default NotificationsScreen;
