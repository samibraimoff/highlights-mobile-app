import { Text, View } from "react-native";

const SearchScreen = () => {
  return (
    <View className={"flex justify-center items-center"}>
      <Text className={"text-red-600 text-2xl"}>Search</Text>
    </View>
  );
};

// export default SearchScreen;
